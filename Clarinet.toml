[project]
name = "dimensional-commitment-engine"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/dimensional-commitment-engine/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.dimensional-commitment-engine]
path = "contracts/dimensional-commitment-engine.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
